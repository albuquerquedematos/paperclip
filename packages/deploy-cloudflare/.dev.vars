# Local dev overrides — loaded by `wrangler dev` automatically
# Override authenticated mode so requests don't require login tokens
DEPLOYMENT_MODE=local_trusted
DEPLOYMENT_PLATFORM=cloudflare
DEPLOYMENT_EXPOSURE=private

# Sidecar = the local Node server (runs plugin jobs, etc.).
# `pnpm dev:cf` (scripts/dev-cf.mjs) injects SIDECAR_URL via wrangler --var
# pointing at whichever free port the server picked, so this file does NOT
# need a SIDECAR_URL line under that flow. Set one manually only if you run
# wrangler dev directly without the orchestration script.
SIDECAR_API_KEY=dev-local
PAPERCLIP_CLAUDE_AUTH_MODE=subscription
